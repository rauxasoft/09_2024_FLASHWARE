package com.sinensia.flashware.backend.business.services.impl_BK;

public class TecnicoServicesImpl {
/*
	private final TecnicoPLRepository tecnicoRepository;
	
	public TecnicoServicesImpl(TecnicoPLRepository tecnicoRepository) {
		this.tecnicoRepository = tecnicoRepository;
	}
	
	@Override
	@Transactional
	public Long create(Tecnico tecnico) throws BusinessException {
		
		if(tecnico.getId() != null) {
			throw new BusinessException("El ID del técnico ha de ser NULL", true);
		}
		
		Long id = System.currentTimeMillis();
		tecnico.setId(id);
		
		tecnicoRepository.save(tecnico);
		
		return id;
		
	}

	@Override
	public List<Tecnico> getAll() {
		return tecnicoRepository.findAll();
	}
*/
}
